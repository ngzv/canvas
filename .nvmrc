# .nvmrc
# Node Version Manager（NVM，Node 版本管理器）的配置文件
# 用于指定当前项目所需的 Node.js 版本。确保团队成员或部署环境使用统一的 Node 版本，避免因版本差异导致的兼容性问题（例如某些依赖只支持特定 Node 版本）。

# 当前项目使用的 `Node.js` 版本
# 完整版本号（推荐,精确控制,前面的 `v` 可选）
v20.19.5

# 主版本号（会匹配该主版本下的最新版本,如 `20.x.x` 的最新版）
# 20

# 主+次版本号（匹配该主+次版本下的最新版,如 `20.19.x` 的最新版）
# 20.19