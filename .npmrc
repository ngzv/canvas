# .npmrc
# NPM（Node.js 包管理器）的配置文件
# 用于配置 NPM 的行为，比如设置镜像源、配置代理、指定依赖安装路径、设置权限等。

# NPM 镜像配置 - 使用淘宝镜像加速国内访问
registry=https://registry.npmmirror.com

# 确保包锁文件被正确生成和使用
package-lock=true
# 强制使用 package-lock.json 中的版本
lockfile-version=3
# 保存新包时自动记录到 package.json
save=true

# 安全配置 - 不允许从不受信任的源安装脚本
audit=true
# 安装前进行依赖检查
preinstall=echo "正在安装依赖，请稍候.."

# 提高安装速度的配置
# 并行安装的最大包数
maxsockets=50
# 缓存设置
prefer-online=0

# 私有包配置（如需使用私有包，取消注释并修改）
# @scope:registry=https://registry.yarnpkg.com/
# //registry.yarnpkg.com/:_authToken=${NPM_AUTH_TOKEN}

# 忽略可选依赖错误
# optional=true

# 开发环境配置
include=dev

# 启用严格模式
enforce-semver=true

# 禁用自动更新提示
update-notifier=false